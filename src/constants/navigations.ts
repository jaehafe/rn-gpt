const authNavigation = {
  AUTH_HOME: 'AuthHome',
  LOGIN: 'Login',
} as const;

const chatNavigation = {
  NEW: 'New',
  DALLE: 'Dalle',
  EXPLORE: 'Explore',
} as const;

export {authNavigation, chatNavigation};
